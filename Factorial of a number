Q. Write a program that can find the factorial of a given number provided by the user.

----------------------------------------------------------------------------------------------------------------
Solution:

number=int(input("enter the numbre:"))
sum=1
for i in range(1,number+1):
    sum=sum*i
print("factorial of the",number,"is =",sum)

-----------------------------------------------------------------------------------------------------------------
output:

enter the numbre: 5
factorial of the 5 is = 120
